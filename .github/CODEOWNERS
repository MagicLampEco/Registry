# Chủ sở hữu mã — ai buộc phải xem trước khi gộp.
#
# ⚠ ĐỌC TRƯỚC KHI TIN TỆP NÀY.
#
# Kho này PRIVATE trên gói GitHub Free. Trên nền đó CODEOWNERS **không** tự yêu cầu review và
# **không** chặn được gộp — GitHub chỉ cưỡng chế nó khi kho public, hoặc khi gói lên Pro / Team /
# Enterprise.
#
# Kho đã chuyển từ tài khoản người dùng `MagicLampNetwork` sang tổ chức, và **tổ chức đó đã đổi
# tên MỘT LẦN NỮA**. Chuỗi đúng hôm nay là `MagicLampEco`. Hai tên đã chết, đo 2026-08-30:
#     gh api users/MagicLampNetwork  → 404   (tài khoản đó nay tên `MagicResearch`)
#     gh api orgs/MagicLampEcosystem → 404
# Đừng gõ tên nào trong hai tên đó vào tệp này, và đừng tin một đường dẫn chỉ vì nó mở được:
# GitHub vẫn CHUYỂN HƯỚNG đường cũ, nên `git fetch`/`git push`/`gh api` bằng tên chết đều XANH.
# Chuyển hướng tắt ngay khi ai đó lập một kho mới trùng đúng đường cũ — lúc đó một lần đẩy bằng
# URL cũ đi vào kho của người lạ, và không dòng nào báo trước.
#
# `@AladinContract` phải là thành viên org có quyền ghi thì GitHub mới coi là chủ sở hữu hợp lệ —
# một owner không đủ quyền bị **bỏ qua trong im lặng**, không báo lỗi. Đo 2026-08-17:
#     GET /repos/MagicLampEco/Registry/collaborators/AladinContract/permission → "write"  ✓
# Đo lại sau khi chuyển, 2026-08-17 — **chuyển tổ chức KHÔNG gỡ được ràng buộc này**:
#
#     GET /repos/MagicLampEco/Registry/rulesets
#     → 403 "Upgrade to GitHub Pro or make this repository public to enable this feature."
#     GET /orgs/MagicLampEco  → plan "free", private true
#
# Ghi rõ vì bản trước của chú thích này gợi ý sai nguyên nhân. Cửa khoá **không** phải "vì chủ là
# tài khoản người dùng, không có team". Cửa khoá là **gói free + kho private**, và đó là một phép
# nhân: gỡ một trong hai vế thì mở. Chuyển tổ chức chỉ đổi thứ thứ ba, nên nó không mở gì cả.
#
# Nên hôm nay tệp này là **tài liệu về trách nhiệm**, chưa phải cổng. Cổng thật đang chạy là
# `.github/workflows/kiem-ho-so.yml` — nó chấm hồ sơ, bắt `platform_id` trùng và kiểm kiểu, và nó
# chạy được trên gói Free. Đừng đọc tệp này như một lá chắn cho tới khi một trong hai vế kia được
# gỡ; lúc đó bật "Require review from Code Owners" thì nó có hiệu lực ngay, không phải sửa gì ở đây.

# Mặc định
*                           @AladinContract

# Hồ sơ đăng ký — chỗ một chữ ký cấp cho người ngoài quyền mang tên hệ đi thuyết phục người dùng.
# Tính duy nhất `platform_id` không có bảo đảm mật mã (`Specs/Math-Spec.md` §14 L1); nó dựa vào
# đúng hai thứ: phép kiểm trùng trong CI, và người ký không ký hai lần cùng một tên.
/Registrations/             @AladinContract
/REGISTRATION-STANDARD.md   @AladinContract

# Validator — sửa là đổi script hash là đổi địa chỉ. Xem `Specs/Math-Spec.md` §17 để biết cách so
# hash trước/sau.
/onchain/                   @AladinContract

# Đặc tả — nguồn của mọi bất biến mà cả hệ đang bám.
/Specs/                     @AladinContract

# Chính cổng gác. Sửa được cổng thì lách được mọi thứ cổng gác.
/.github/                   @AladinContract
/tools/                     @AladinContract
